ams:
  base-url: "http://localhost:8181"

kc:
  realm: orchestrator-realm
  base-url: ${KEYCLOAK_BASE_URL}
  realm-url: ${kc.base-url}/realms/${kc.realm}
  user: user
  password: password

spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: orchestrator
            client-secret: rSNaQ1BQuKgOBzFabLvghl2lLQWI07rw
            scope: openid, profile